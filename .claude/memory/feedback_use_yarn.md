---
name: Use yarn for package management
description: This project uses yarn, not npm
type: feedback
---

Always use `yarn` instead of `npm` for this project.

**Why:** User preference / project convention.

**How to apply:** Use `yarn lint`, `yarn build`, `yarn tsc`, etc. — never `npm run ...` or `npx ...`.
